// EventManagerPanel — simplified event management for promoters and clubs.
// Renders PromoterDashboard filtered to the logged-in user's own events.
export { default } from '@/components/PromoterDashboard';
